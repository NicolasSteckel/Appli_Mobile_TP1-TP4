<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.formulaire_nicolas_steckel">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_form"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_form_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Formulaire_Nicolas_Steckel">
        <activity android:name=".Splash_activity" android:theme="@style/SplashTheme" android:parentActivityName=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".Activity2" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Formulaire_Nicolas_Steckel.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>